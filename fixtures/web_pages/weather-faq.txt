天气查询常见问题（FAQ）

问：为什么同一城市不同网站的温度不一样？
答：各家数据源采集站点、更新频率与算法不同，短时差异属正常。

问：wttr.in 是什么？
答：一个把天气渲染成纯文本/ASCII 的开源服务，适合命令行 `curl wttr.in/城市`。

问：如何只查某地未来三天预报？
答：多数 API 支持 days 参数；wttr.in 默认返回当天加未来两天概览。

提示：查询参数应只含裸地名，不要附带"天气/weather"等修饰词。
